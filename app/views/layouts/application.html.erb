<!DOCTYPE html>
<html>
  <head>
    <title>DreamJournalApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application","app", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <%= favicon_link_tag asset_path("logo/CA-logo.png") %>

  </head>

  <body>
    <div class="container-fluid">
      
      <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
          <a class="navbar-brand" href="<%= home_index_path %>"><%= image_tag("logo/CA-logo.png", class:"navbar-logo img-fluid")%></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ">
              <li class="nav-item">
              <%= link_to 'Home', home_index_path, class:"nav-link"%>
              </li>
              <li class="nav-item">
              <%= link_to 'Dream Log', dreams_url, :method => :get, class:"nav-link"%>
              <% if user_signed_in? %>
              <li class="nav-item">
              <%= link_to 'Sign Out', destroy_user_session_path, :method => :get, class:"nav-link"%>
              </li>
              <% else %>
              </li>
              <li class="nav-item">
              <%= link_to 'Register', new_user_registration_path, class:"nav-link"%>
              </li>
              <li class="nav-item">
              <%= link_to 'Log In', new_user_session_path, class:"nav-link"%>
              </li>

              <% end %>
            </ul>
          </div>
        </nav>
      </header>

      <%= yield %>

    </div>
  </body>
  
      <footer>
        <div>
          <% if user_signed_in? %>
          </br>
          <% else %>
          </br>
            <%= link_to 'Register', new_user_registration_path %> or
            <%= link_to 'Log in', new_user_session_path %>
          <% end %>    
          @Copyright Dream Journal 2022      
        </div>

      </footer>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</html>
